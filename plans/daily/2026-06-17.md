# 2026-06-17 · Coordinator · v2.0.7 token 审计 + bug 修复收尾

> **当日主题**：把 v2.0.7 demo 修到稳态 + 做完整 token 审计
> **关联**：`docs/token-audit.md` / `docs/v2.0.7-llm-architecture.md` / `plans/progress.md` v0.5

## 昨天说要做（06-16 续）
- [x] v2.0.7.1 合并 Router+Skill 为 1 次 LLM
- [x] 加 Router 缓存（30-50% 重复节省）
- [x] 走查剩余 bug + 修
- [x] 完成 token 审计文档

## 今天做的

### 1. AI 架构落地（v2.0.7.1 → v2.0.7.2）
- 重写 `src/lib/ai/orchestrator.ts`：2 次 LLM 串行 → 1 次合并调用
- 新增 `src/lib/ai/prompts/unified.ts`：单 system prompt 三层共用（Router + Skill + Response）
- 新增 `src/lib/ai/cache.ts`：5 分钟 TTL，crisis/drill 不缓存
- 备用 `prompts/{router,skill-drill,skill-free-dialogue,skill-crisis}.ts`（hot-swap 兜底）
- 旧 `prompts/{analyze,reply}.ts` 标废弃（0 引用）
- API 路由支持 `intent_override` 字段（演练步骤 9 直接传 `'drill'` 跳过 Router）

**成果**：单次请求 ~6000 → ~419 tokens（**-93%**），延迟 50s+ → 8s

### 2. Bug 修复（v2.0.6 polish）
- **/progress 自报** `Link href="/api/progress?bucket=X"` → client-side POST + 乐观更新 + 失败回滚 + 计数显示
- **/drill 步骤 11 保存** catch 静默 → 3 态反馈（saving/saved/failed）
- **/drill 步骤 5 双选** onPick 立即 setStep(6) → onChange 不前进 + 下一步按钮
- **/scripts 不能删** → Trash2 按钮 + 二次确认 + 详情可展开
- **/settings 刷新设置丢** → localStorage 持久化（`nw-settings-v1`）
- **BottomNav/GlobalHeader** 无焦点态 → `focus-visible:ring-2 ring-primary`
- **§1 觉察节点点击没反应** → 补 3 个 L1 场景（`l1-body-awareness` / `l1-emotion-naming` / `l1-pattern-spotting`）
- **fallback 重复"我在听"** → v2.0.3 buildContextualFallback：按 turn 切换 + 引用用户原话 + turn ≥ 3 加 silent_retry hint
- **空消息** → NW-CO-0010（400）；**超长** → NW-CO-0011（422）
- **/api/profiles/me 404** → 补建子路径
- **PATCH /api/scripts/[id] 500** → dev 冷编译瞬时态（非真 bug）
- **scripts POST/GET 不一致** → globalThis 共享 store
- **drill `<Link>` ReferenceError** → import Link

### 3. 演练改 Duolingo 全选择题
- 步骤 3/4/5/7 从自由输入改为「4 选 1 + 其他」+ 「换一句」
- 新增 `MultiChoice` 通用组件
- 7 个场景补全 `StepOptions`（emotions / wants / wills / wonts / openers / user_says_variants / counter / counter_replies）由 PE+KE 联合签字

### 4. token 审计完成
- `docs/token-audit.md`（305 行）：6 章节 + 9 步优化路线图
- 关键发现：v2.0.7.2 已达 90% 节省目标
- 剩余 7% 边际效用低，按需推进

## 决定 / 阻塞 / 风险

### 决定
- ✅ **Router+Skill 合并为 1 次 LLM**（替代 v1.0 的 2 次串行）
- ✅ **crisis 路径不调 LLM**（规则层 + 本地模板）
- ✅ **KB 全文不注入 LLM**（只传 id，前端按需 `GET /api/kb/[id]`）
- ✅ **历史消息不注入 LLM**（当前实现，避免无限增长）
- ⏸️ **History 摘要机制延后**（当前不需要，加了反而 +30 token）

### 阻塞
- 无

### 风险
- ⚠️ **M3 LLM 流截断 ~33%** — v2.0.7 后路由 fallback 已加固，但端到端仍需流式 SSE（M2 PoC 任务 11）
- ⚠️ **gpt-3.5-turbo 偶发返回空** — 0 引用 risk，应该加 retry with backoff

## 明天（v2.0.7 收尾 → M2 PoC 启动）

1. **PE 启动 prompt 重写**（T-09-06）：12 步每步人格注入（依赖 4 份老 prompt.md）
2. **FE 4 张高保真稿**（design-review-jobs.md §9）：主页 / 演练 / 收束 / 进度
3. **M2 PoC 端到端**（plans/m2-poc.md T-01~T-17）：1 微技能 + 12 步端到端

## 进度同步

- `plans/progress.md` v0.4 → v0.5：v2.0 demo 端到端可用
- `plans/m2-poc.md`：17 子任务均 pending
- 测试：11 → 22（+100%）
- 0 拒绝清单破窗

## 关键数字

| 指标 | 06-16 | 06-17 | 变化 |
| --- | --- | --- | --- |
| System Prompt 数 | 4 (1.3k each) | 1 (854 tokens) | -75% |
| LLM 调用/req | 2 | 0.7 (含 cache) | -65% |
| 单次延迟 | 50s+ | 8s | -84% |
| 1000 DAU 成本/天 | ~$20+ | ~$2.8 | -86% |
| 测试 | 11 | 22 | +100% |
| 已修 bug | — | 14 | 14 个 |

## 学到了什么

- **Next.js dev 缓存会让二分定位 bug 不可靠** — 改完文件后 SWC 编译有 5-30s 滞后；遇到"should work"但不 work 的情况，重启 dev server 强制重新编译最稳
- **TS 编译过但 SWC 失败** — 大概率是某个 Unicode 字符或 JSX/SWC 微妙不兼容（不是 TS 问题）；用"最小可编译模板逐步加"的方法最快定位
- **in-memory Map + globalThis** 是 dev 模式最简的"跨模块共享状态"；生产接 Supabase 后这段直接废弃
- **3 层架构 + 1 次 LLM** 比 2 层架构 + 2 次 LLM 更省；function-calling 模式让"路由"和"技能"自然合并输出
